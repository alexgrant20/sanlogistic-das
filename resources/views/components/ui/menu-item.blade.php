<div class="d-flex justify-content-center">
  <a href="{{ $link }}" class="d-flex flex-column align-items-center">
    <i class="{{ $icon }} fs-1 mb-3 {{ $backgroundColor }} rounded py-2 px-4"></i>
    <span class="fw-bold fs-5 text-center">{{ $label }}</span>
  </a>
</div>
